touch .gitignore
git config --global user.name kalpana
git config --global user.email aba@gmail.com
git init
git add .
git commit -m "Initial Commit"
git status
$ ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
tail <path to id-rsa.pub> add this key github account\
git remote add origin <ssh url.git>
git push origin master
